/* ============================================================
   DipMapper — Anzeigenplätze
   ------------------------------------------------------------
   Solange die drei Felder leer sind, laufen nur AUTOMATISCHE
   ANZEIGEN (Google platziert sie selbst). Das ist völlig ok.

   Wenn du später feste Plätze willst:
     AdSense -> Anzeigen -> Nach Anzeigenblock
              -> Displayanzeigen -> erstellen
     Google zeigt dir Code mit  data-ad-slot="1234567890"
     Nur diese Zahl hier zwischen die Anführungszeichen setzen.

   Danach diese Datei allein neu hochladen. Sonst nichts.
   ============================================================ */
window.DM_ADS = {
  client: 'ca-pub-2870312071451207',
  header: '',   // Anzeige oben, unter der Kopfzeile
  mid:    '',   // Anzeige unter der Karte
  footer: ''    // Anzeige unten, über dem Rechtlichen
};
